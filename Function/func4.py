# function to check whether a number falls within a given range.

def in_Range(a):
    if a in range(2,9):
        print(a," is in range")
    else:
        print(a," is not in the range")
in_Range(4)